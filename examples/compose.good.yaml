services:
  api:
    image: internal/payments-api:2.4.1
    user: "10001:10001"
    read_only: true
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    mem_limit: 512m
    cpus: "1.0"
    ports:
      - "8080:8080"
